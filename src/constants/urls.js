const API = 'https://clickedin.net/api/v1/'

export const CHANGE_PASSWORD = `${API}users/change-password`
export const LINKEDIN_CREDS = `${API}users/linkedin-creds`
export const SIGNUP = `${API}/signup`
export const LOGIN = `${API}/login`
export const LOGIN_WITH_TOKEN = `${API}/token`
